# RAW AROMACHEM — Build State

## ⚑ REALIGNMENT (2026-06-25, authoritative) — Phase-1A Data Dictionary
The owner provided the **Phase-1A Manufacturing Core MVP Scope Freeze v1.0 + Data Dictionary (~150 tables)** as the REAL Phase-1 spec. We now **realign the data+service layer to it table-for-table** (plan: `PHASE1A_SCHEMA_PLAN.md`), keeping the architecture (clusters/edge/outbox/vault/workflow). The prior `@ra` clusters below were an *approximate* schema and are being superseded by dictionary-faithful schemas.
- **Conventions:** snake_case 1:1 map of the dict; `dictPk("<entity>_id")` + `metaColumns()` (status, created_dt, updated_dt, created_by/updated_by varchar) added to `@core/data-kernel`. In-cluster = real FK; cross-cluster = soft ref.
- **Locked decisions:** IssuedQty = BOOLEAN (flag); vault = encrypt-at-rest faithful to FORMULA_VAULT.EncryptionKeyRef; scope = 10 modules, Phase-1B excluded (no returns/complaints/CAPA-workflow/dashboards/notification-engine).
- **✅ DATA LAYER COMPLETE — 139 tables / 12 schemas, all typecheck-green + adversarially verified table-for-table vs the dictionary.** Built via: foundation `data-org` (iam, 11) by hand as the template; then 2 parallel workflows (build 6 new: reference 13, location 13, production 15, packaging 11, sales 6, workflow-state 2; rewrite 5 in place: masterdata 9, procurement 20, inventory 19, quality 7, formula 13). Conventions honored: dict `soft` marking authoritative (only header→line `FK→` are real FKs); IssuedQty boolean; formula ingredients encrypted (enc_payload/iv/tag) + hash-chained audit. Full dict persisted at `docs/PHASE1A_DATA_DICTIONARY.md`.
- **✅ Clean baseline restored:** removed the 13 obsolete packages (5 @ra cluster + 5 @ra contracts + 3 @mfg); reset app.module/worker.module/api to the @core foundation (identity + platform + edge/outbox). **Full typecheck green, boundaries clean (284 modules).** App boots on @core auth/platform; the 139-table dict data layer sits ready underneath, unwired.
- **✅ FOUNDATION SERVICES DONE (green, 323 modules):** built 3 dict service clusters (parallel workflow + verify) and wired into api: `@ra/cluster-org` (iam CRUD: org/user/role/permission/mappings — 11 tables — + **auth on USER_MASTER**: `POST /auth/login` Argon2id, roles via user_role_mapping→role_master, perms via role_permission_mapping→permission_master, mints the @core JWT; `POST /auth/users/:id/password`; `GET /me`), `@ra/cluster-reference` (13 platform masters CRUD), `@ra/cluster-location` (13 warehouse-hierarchy CRUD). Each: own DB client from PG_CLIENT, per-table create+list+get, per-arg zod, RBAC-gated, public-api lookup (ORG/REFERENCE/LOCATION_LOOKUP). **Retired @core/cluster-identity** (replaced by dict auth; avoided /auth/login route clash). App boots on the dictionary foundation.
- **✅ PROCURE-TO-RACK SERVICES DONE + WIRED (green, 386 modules, boundaries 0 violations):** built 4 dict service clusters and wired all into api(HTTP)+worker(outbox) in one pass — `@ra/cluster-masterdata` (9 tables CRUD; emits `masterdata.material.created`/`alias.created`; `MASTERDATA_LOOKUP` findMaterial/findAliasForMaterial), `@ra/cluster-procurement` (20 tables CRUD + flow: PR submit/approve, PO create-from-quotation/approve/issue/acknowledge with total_amount=Σ(amount), emits `procurement.po.issued`; `PROCUREMENT_LOOKUP`), `@ra/cluster-inventory` (19 tables CRUD + flow: gate→GRN→rm_batch→batch_container_mappings, rm-batch release→inventory_batch+transaction+event_history, stock ops; emits `inventory.grn.created`/`inventory.batch.created`; `INVENTORY_LOOKUP` findRmBatch/findInventoryBatch), `@ra/cluster-quality` (7 tables CRUD + flow: inspection create/results/dispose; emits `quality.qc.passed`/`failed`; `QUALITY_LOOKUP`). Built via 2 parallel workflows (1st pass: procurement+quality green; masterdata+inventory failed on transient API errors → rebuilt in a focused 2nd workflow); each adversarially verified, then **central real-tsc typecheck + boundaries green** (workflow verifiers can't run tsc — must verify centrally). Both processes now boot the full procure→QC→rack spine on the dictionary schema.
- **✅ FORMULA VAULT DONE + HARDENED + WIRED (green, 403 modules, boundaries 0 violations):** built BY HAND (crown-jewel IP, not delegated) — `@ra/cluster-formula`, 13 tables. Crypto: per-formula 32-byte DEK, AES-256-GCM envelope, DEK wrapped under the KEK via `KmsPort` (EnvKmsAdapter over `FORMULA_KEK`; real-KMS swap-seam). Ingredients seal {materialId,percentage} → enc_payload/iv/tag; structural cols stay plaintext. OWN `ra_vault` connection (FORMULA_DATABASE_URL / falls back to DATABASE_URL) = role-isolation wall + in-house seam. Masking: `FORMULA_LOOKUP` port — `getFloorView` (RM_ALIAS + % only, real material_id dropped) vs `getPickList` (real, SERVER-SIDE only, no HTTP route). Every decrypt routes through `VaultService.decryptVersion`: approved-locked-only + a hash-chained `formula.audit_events` row in the SAME tx (no un-audited read). Emits `formula.version.approved` (→ production Formula Selection) + `formula.copy.approved`. **Adversarially red-teamed (3 parallel reviewers: crypto / leak-surface / audit+access); confirmed: floor + non-owner cannot obtain real material_id.** Fixed 8 findings: TOCTOU lock on seal (FOR UPDATE inside tx), per-formula owner/access-policy gate on `/actual`, monotonic `chain_seq` folded into the audit MAC (added to `@core/data-kernel` auditTable — additive, nullable), injective JSON audit-canonical (covers seq/requestId/ip), approve-replay ConflictException, DRAFT allowlist, wrapped-key shape + strict-base64 validation, audit on createVersion/createStage. **Deferred (tracked, not blocking):** seed `formula:*` perms to roles (`formula:actual:read` → owner only) — cross-cutting @ra IAM-seed task; external audit-head checkpoint (in-DB-only tail-deletion needs an off-DB anchor); role-scoped access policies need role-id threaded into the JWT principal; thread request-id/ip into the vault audit calls.
- **✅ PRODUCTION + PACKAGING + SALES DONE + WIRED — FULL 23-STEP FLOW COMPLETE (green, 450 modules, boundaries 0 violations):** built via an orchestrated Workflow (1 coherent build agent per cluster + 1 adversarial verifier each; ultracode), then central real-tsc + boundaries. `@ra/cluster-production` (15 tbl) — planning expands an APPROVED formula into the order's BOM **server-side via `FORMULA_LOOKUP.getPickList`** (ForbiddenException if not approved; percentage consumed transiently, never persisted/exposed; getPickList has no HTTP route), then pick-list→material issue (flips BOOLEAN issued_qty)→secure mixing→oil batch (+consumption genealogy +event history)→production QC; emits production.order.created/materials.issued/oil_batch.created/qc.recorded. `@ra/cluster-packaging` (11 tbl) — product/SKU/material/BOM masters→package order (BOM-expands package_order_item, BOOLEAN issued_qty)→filling session/details→FG batch (+consumption); emits packaging.order.created/filling.done/fg_batch.created. `@ra/cluster-sales` (6 tbl) — customer/transporter masters→sales order (+items, totalAmount=Σ)→confirm→dispatch (+items vs FG batch); emits sales.order.created/confirmed/dispatch.created. Found+fixed a data-layer gap first: production/packaging/sales schemas were MISSING the crosscutting outbox/audit pair — added + barrel-exported. Verifiers: production + packaging PASS 0/0; sales PASS structurally with 1 HIGH = the systemic `@ra:*` permission-seeding gap (below). Flow is operator-driven (no auto-projectors needed between order steps; events are for traceability).
- **⚠️ CROSS-CUTTING (flagged twice — vault red-team P1 + sales verifier HIGH): `@ra:*` permissions are guarded on every route but seeded nowhere.** RA auth mints JWT perms from the dict tables (permission_master ← role_permission_mapping ← role_master); until those are seeded, no @ra route is grantable to any role (routes fail closed). This is REQUIRED for the app to function beyond /auth/login + /me, and belongs with the migrate/seed step (role→perm grants are a product decision: e.g. owner=all, floor=alias-only reads, no `formula:actual:read`). **NOT a code bug — a pending seed-data task.**
- **✅ GO-LIVE TOOLING READY (drizzle-kit generate blocker RESOLVED):** the CLI can't resolve the NodeNext `.js` schema specifiers; fixed by using drizzle-kit's PROGRAMMATIC api under `tsx` (load `drizzle-kit/api` via `createRequire` → the CJS `api.js`, since the ESM `api.mjs` ships a broken `__require` shim). Scripts: `scripts/db-schema-groups.ts` (12-group map; iam=@core/data-iam+@ra/data-org, platform=@core/data-platform+@ra/data-reference merged so push doesn't diff-drop; formula→FORMULA_DATABASE_URL), `db-validate.ts` (offline snapshot — **VERIFIED: 12 groups, 185 tables, 0 fail**), `db-push.ts` (`pnpm db:push` — prereqs uuidv7+pg_trgm+postgis, then pushSchema per group; refuses data-loss without ALLOW_DATA_LOSS), `db-seed.ts` (`pnpm db:seed` — 263 perms→permission_master, owner role, grant ALL incl `formula:actual:read`, Argon2id bootstrap owner login, mappings; idempotent), `ra-permissions.ts` (263-perm catalog, grep-generated, max 49 chars ✓ fits varchar(50)). `.env.example` (all vars + secret-gen one-liners), `migrate-all.sh` → delegates to db:push+db:seed. Both push+seed load clean under tsx (verified; correctly demand DATABASE_URL). Root package.json got the tooling+data-pkg+argon2 devDeps.
- **🟢 LIVE ON NEON (2026-06-25) — full stack verified end-to-end.** Owner provided a Neon `DATABASE_URL` (single DB; vault shares it, FORMULA_DATABASE_URL unset). Used the **direct** endpoint (not `-pooler`) + `prepare:false` for stable DDL/prepared-statements. `pnpm db:push` created all 12 schemas / **185 tables** (714 statements) — needed two fixes vs the first attempt: (a) `generateMigration(empty→target)` instead of `pushSchema` because drizzle-kit 0.30.6's `fromDatabase` introspector throws under drizzle-orm 0.38 + PG16/17 (`allTables.map` of undefined); (b) filter the generated bare `CREATE SCHEMA` (we create it `IF NOT EXISTS`). `pnpm db:seed` loaded 263 perms + owner role (all perms) + Argon2id owner login. **Runtime DI fix:** the worker's `OUTBOX_SOURCES` factory needs each cluster's `<X>_DB` GLOBAL (kernel assumption) — marked the 8 outbox-source cluster modules `@Global()` (masterdata/procurement/inventory/quality/formula/production/packaging/sales). **API boots clean** (`node --env-file=.env … src/main.ts`, RUN_WORKER_IN_PROCESS=true, in-process worker + all 11 clusters init). **Smoke tests all pass:** POST /auth/login → 201 JWT (portal=owner, 263 perms incl formula:actual:read); authed GET /v1/formula-types → 200; no-token → 401 fail-closed; POST /v1/formula-types → 201 persisted (uuidv7, ACTIVE, createdBy=owner); GET /me → 200. **The 23-step manufacturing platform is operational against live infra.** Secrets/DB-url were exchanged in chat → ROTATE before any public/production deploy; `.env` is gitignored.
- **✅ RBAC ROLES SEEDED + VERIFIED (2026-06-25):** `scripts/ra-roles.ts` defines the role→perm matrix; `db-seed.ts` seeds owner(263, ALL) + **floor(41)** + **qc(23)** + **procurement(77)** with `RoleDef.select` predicates, and enforces a HARD INVARIANT (throws if any non-owner role's grant includes `formula:actual:read` = `OWNER_ONLY_PERMISSION`). Sample logins created per role (floor@/qc@/procurement@rawaroma.local). **Verified live:** floor /me has NO `formula:actual:read`; floor→`/v1/formula-versions/:id/actual`=403, owner same=404 (passed guard); floor→production-orders=200, floor→vendors=403, procurement→vendors=200. The floor is provably locked out of the decrypted recipe.
- **✅ RESPONSE-LEVEL ALIAS MASKING DONE + VERIFIED + RED-TEAMED (2026-06-25) — the BFF ResponseProjection layer (was deferred):** `MaterialMaskingInterceptor` (backend/api/src/masking/) is a global APP_INTERCEPTOR registered AFTER ResponseEnvelopeInterceptor (so it masks the raw handler payload before the envelope wraps). For any caller WITHOUT the new `masterdata:material:reveal` capability, it recursively strips every `materialId` → null (FAIL-CLOSED: real id gone even if alias lookup fails) and attaches the RM alias (`rmAliasId`+`aliasName`) via `MASTERDATA_LOOKUP.findAliasesForMaterials` (batched, chunked 500). Pure helpers in mask.ts (isPlainObject guard preserves Date/Buffer/null/false). Capability seeded as `CAPABILITY_PERMISSIONS` (ra-roles.ts) + granted to owner/qc/procurement, NOT floor. **Live-verified end-to-end** (full chain: material→alias→formula→seal→approve→production order BOM expand via vault getPickList→masking): owner GET order-ingredients = real materialId; qc GET /v1/materials = real materialId; floor GET order-ingredients = `materialId:null`+`aliasName:"ING-A001"`. **Adversarially red-teamed (2 reviewers): verdict = no floor user can obtain a real material_id via any reachable endpoint** (success+error paths). Hardened 2 items (principal optional-chain vs 500; chunked the IN-list). TRACKED latent (no live leak): error-path messages aren't masked (no floor handler embeds materialId today — keep it that way / add a scrubber); add a floor-JWT regression test once a test harness exists; isPlainObject is fail-open for class-instance/Map shapes (none returned today); keep alias names OPAQUE (ING-codes, not real names) — operational.
- **Deploy to public host (next):** the live instance runs LOCALLY against the Neon prod DB. To expose it publicly, deploy `@core/api` to Render/Fly/etc. (host-agnostic) with the same env vars; one service (api+worker) since RUN_WORKER_IN_PROCESS=true. Set APP_ENV=prod + CORS_ORIGINS when frontends exist. ROTATE secrets (chat-exposed) first.
- **Remaining after live:** floor/QC/procurement roles with narrower perm grants (floor must NOT get `formula:actual:read`); optional verifier-LOW hardening (confirmSalesOrder idempotency; external audit-head checkpoint; role-scoped formula policies need role-id in JWT; thread request-id/ip into vault audit); UI portals.

---
*(historical — the approximate build below predates the realignment; schemas being rewritten to the dictionary)*

Tracks Phase-1 execution against `../../M1_Deliverables/PHASE1_BUILD_PLAN.md` (BUILD-RAC-P1 v1.0).
Scope = the 12 frozen modules, no drift (see plan §0.1). Reuse tiers: `@core/*` (kernel, reused) · `@mfg/*` (manufacturing-generic, graduated later) · `@ra/*` (perfume domain).

## Phase ledger

| Phase | Name | Status | Notes |
|---|---|---|---|
| **P0** | Bootstrap | 🟡 in progress | ✅ Cloned @core → `Raw_Aroma/app`; git re-init; renamed `raw-aroma`; tokens reskinned (gold/charcoal, dark owner console); `pnpm -r typecheck` **green (15/15)**. ✅ **Modular-monolith enforcement** — `.dependency-cruiser.cjs` (3-tier one-way `@core→@mfg→@ra`, cluster-public-api-only, no-cycles) + `pnpm boundaries` **passing (188 modules, 0 violations)**. ✅ **In-process worker switch** (`RUN_WORKER_IN_PROCESS` → boots `WorkerModule` in the api process; free single-service deploy). ✅ BFF delta — **fail-closed CORS** (`CORS_ORIGINS` in config; prod boot-errors on unset; LEAK-8). Remaining BFF deltas: `ResponseProjectionInterceptor`+per-role schemas, httpOnly refresh cookie+CSRF, CF headers/CSP + `removeConsole`/no-sourcemaps, log-redaction. Then CI + Data-Integrity gate, deploy files (render.yaml/Dockerfile/migrate-all). Infra wiring needs owner's Neon/Render/Vercel/Resend accounts. |
| **P1** | Foundation (M01) | 🟡 in progress | ✅ **Full masterdata vertical slice built & wired** (typecheck green 211 modules, boundaries clean): `@ra/data-masterdata` (7-level warehouse self-tree; material master+classification(soft-ref master_items)+alias(Charter C-5 CHECK)+storage rules(Charter C-7 humidity); contact master(C-7 comm-prefs+social); outbox+audit; migrate+seed). `@ra/contracts-masterdata` (DTOs+events `masterdata.material.registered`/`alias.updated`/`storage_location.created`/`contact.assigned`+MASTERDATA_PERMISSIONS). `@ra/cluster-masterdata` (NestJS module, own MASTERDATA_DB from shared pool, MaterialsService/StorageService with transactional outbox, RBAC controllers `/v1/materials` + `/v1/storage-nodes`, `public-api.ts` MASTERDATA_LOOKUP port). Wired into api(HTTP)+worker(outbox source). **2 clean @core improvements** (kept domain-free): open `Permission` type (`| (string & {})`) so @ra owns its perms; variadic `provideOutboxSources([{cluster,dbToken,table}])`. Next: org tree, documents, RBAC scoping, login history, the 8-portal set. |
| — | ⚠️ Known toolchain item (deploy-time) | — | `drizzle-kit generate` fails with `Cannot find module './_schema.js'` — a **pre-existing template quirk** (`@core/data-platform` fails identically; its committed `drizzle/` SQL was generated under a different setup). Resolve migration generation for all @ra packages when provisioning Neon (likely a tsx-loader or drizzle-kit config tweak). Does NOT affect code correctness (typecheck/boundaries green). |
| **P2** | Workflow (M11) + Notifications (M12) | 🟡 in progress | ✅ **`@mfg/kernel-workflow` built** — the **reusable `@mfg/*` tier now exists** (boundary-enforced: `mfg-no-ra`, cluster-public-api-only). `@mfg/data-workflow` (sla_policies, approval_tasks, workflow_transitions; seed includes the **PO ₹25,000 auto-approve** policy = settled O5). `@mfg/contracts-workflow` (approval events + decide DTO). `@mfg/cluster-workflow` (WorkflowService requestApproval/decide/processSlaBreaches with transactional outbox; `/v1/approval-tasks` + decide controller; **worker-only SLA scheduler** @Interval, deadline-in-DB catch-up-on-wake; `WORKFLOW` request port). Wired into api+worker. Typecheck green, boundaries clean (247 modules). Next P2: `@mfg/kernel-notifications` (dashboard-alert inbox + Resend `EmailPort`). |
| **P3** | Material (M02) + Vendor (M03) | 🟡 in progress | ✅ Material (M02) done in masterdata slice. ✅ **Vendor (M03) full slice** (typecheck green, boundaries clean 228 modules): `@ra/data-procurement` (vendors with Charter C-5 approved-complete CHECK; vendor_material_map; vendor_rate_history with money(); vendor_performance incl. **partial%/response%**; outbox+audit; migrate). `@ra/contracts-procurement` (DTOs + `procurement.vendor.*` events + PROCUREMENT_PERMISSIONS). `@ra/cluster-procurement` (VendorsService with transactional outbox + approve completeness guard, RBAC controllers `/v1/vendors` create/list/approve/map-material/record-rate, `PROCUREMENT_LOOKUP` port). Wired into api+worker. **Reconciliation:** vendor contacts REUSE the masterdata contact master (`owner_type='vendor'`) — no duplicate vendor_contacts table (resolves critic GAP-TBL-2). Remaining M03: vendor-contact UI wiring (uses existing contacts). |
| **P4** | Procure-to-rack (M04+M05+M06) | 🟡 in progress | ✅ **PR + PO approval spine (M04) with full workflow integration** (typecheck green, boundaries clean 254 modules): data — purchase_requests(+lines), purchase_orders(+lines, totals in minor units). contracts — PR/PO DTOs + `procurement.pr.*`/`procurement.po.*` events + perms. cluster — `PurchaseRequestService` (create→submit→`WORKFLOW.requestApproval`), `PurchaseOrderService` (create computes total → submit carries amountMinor for the **₹25k auto-approve gate** → issue → vendor acknowledge), `ApprovalProjector` (worker; subscribes workflow.approval.granted/auto_approved/rejected → transitions PR/PO — the decoupled async outcome). ProcurementModule imports WorkflowModule for the WORKFLOW port. **Boundary lint refined**: cluster may import a sibling's barrel(index)+public-api (for DI wiring), never internals. ✅ **Inventory cluster (M05 gate/GRN/batch + M06)** + **Quality cluster (M05 QC/sample/settlement)** — built in PARALLEL via a Workflow (2 build agents + adversarial self-review each), integrated centrally, **green on first try** (typecheck + boundaries clean, 306 modules). Inventory: gate_entries, grns(+lines, batch_id-on-receipt CHECK), rm_batches(vendor_id NOT NULL, split via parent_batch_id), **stock_movements ledger + inventory_batches projection** (event-sourced, in-tx fold), audits; QcOutcomeProjector (idempotent). Quality: qc_parameter_configs(num_nonnulls CHECK), qc_inspections(inbound⇒grn_id CHECK, subject_type covers rm/oil/fg), qc_results, sample_retentions(inspection_id NOT NULL), vendor_settlements; 6 named QC params seeded; IncomingQcProjector. **Cross-cluster event chain VERIFIED** (strings match): inventory.batch.created→quality auto-creates inbound QC; quality.qc.passed/failed→inventory releases/rejects batch. 🎯 **procure→QC→rack chain now wired end-to-end across 5 clusters.** Added `scripts/migrate-all.sh`. Remaining M04: RFQ→quotation→negotiation (extends procurement). |
| **P5** | Formula Vault (M07) 🔒 | ✅ built (typecheck+boundaries green, 327 modules; security review in flight) | `@ra/data-formula` (own `formula` schema, ENCRYPTED): formula_master, formula_versions (wrapped_dek, locked-on-approval, version_hash), formula_ingredients (plaintext alias/qty + envelope-encrypted real name/%/secret), hash-chained audit_events. `@ra/contracts-formula` (events carry alias/ids ONLY; minimized access_logged; constrained reason DTO). `@ra/cluster-formula`: **KmsPort** + EnvKmsAdapter (AES-256-GCM, KEK from FORMULA_KEK, never in DB), **VaultCrypto** (per-version DEK envelope), **StepUpVerifier** (HMAC, user-bound, ≤5min), **own FORMULA_DB** from FORMULA_DATABASE_URL (role isolation), **single de-tokenization** public-api (`resolveAliasesForProduction`=alias+qty only / `getActualFormula`=owner-only decrypt). 7 red-team must-fixes applied: throwing in-tx hash-chained audit before decrypt; fixed allow-list audit `after`; minimized bus event; constrained reason; step-up; service-level super_admin re-check; vault-internal version hash. config: FORMULA_DATABASE_URL + FORMULA_KEK added. Production will consume FORMULA_VAULT. **ADVERSARIAL SECURITY REVIEW done + code-level fixes applied** (green): (1) getActualFormula now requires approved+locked (was decrypting drafts); (2) keyed-HMAC version hash recomputed+verified before decrypt (tamper detection) via `VaultIntegrity`; (3) prod boot-assertion fails closed if vault shares the app role; (4) step-up single-use (in-memory); (5) audit chain serialized by `pg_advisory_xact_lock` + keyed HMAC (no fork, not plain SHA); (6) honest comments. **Deploy-time follow-ups (noted):** DB REVOKE UPDATE/DELETE on locked versions + audit_events (infra grants); Redis for multi-instance step-up single-use + per-actor rate-limit on /actual (core limiter is a placeholder); real KMS adapter (O3) for host/RCE separation; recordOutbox schema-validation. |
| **P6** | Production (M08) | ⬜ | secure mixing; oil_batch_consumption; oil ledger |
| **P7** | Packaging (M09) + Sales (M10) | ⬜ | FG ledger; returns/complaints thin (optional) |
| **P8** | Traceability + M12 alert views | ⬜ | 🎯 genealogy + recall; 4 alert dashboards only (no analytics) |
| **P9** | Harden + IoT/AI seams + UAT + go-live | ⬜ | seams only; audit-chain verifier; data migration; go-live |

## Settled decisions (all resolved — nothing blocking)
- O1 Hosting = **host-agnostic, decide at go-live**. · O2 Email = **Resend** (free) behind `EmailPort`. · O3 Vault keys = **KMS free-tier now**.
- O5 PO auto-approve = **₹25,000 threshold** — below: auto-approve after 24h; ≥₹25,000: no auto-approve, escalate to Owner (P4).
- O6 Multi-company/residency = **N/A** — no Dubai/foreign entity (that was an old-draft placeholder); single shared DB, single country, multi-location via the org tree (P1).

## Conventions (inherited, do not regress)
- schema-per-cluster · `public-api.ts` only cross-cluster surface · no cross-schema FK (soft refs + events) · UUIDv7 PKs · `recordOutbox(tx,…)` on every state change · hash-chained `audit_events` · per-cluster migrations · kill-switch flag per module · per-role zod output projection (BFF).
