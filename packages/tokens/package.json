{
  "name": "@core/tokens",
  "version": "0.0.0",
  "description": "Per-portal design tokens (semantic vars) → CSS variables + Tailwind preset. Shared by web (CSS) and mobile (NativeWind).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./css": "./src/css.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
